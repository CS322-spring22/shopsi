const user = {
    'waist' : 27,
    'bust' : 34,
    'inseam' : 31,
    'hips': 35,
    'units' : 'in'
}

//need window.onload
window.onload = () => {
    //check which store
    if (document.title.indexOf('Shein') != -1){
        //find size guide
    }
    else {

    }
    //  get user measurements
    //  compare user measurements to size guide
    //  click on correct size
}